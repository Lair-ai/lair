apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.namespace }}-tika
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: tika
  ports:
    - port: 80
      targetPort: 9998  # Apache Tika default API port
  type: ClusterIP